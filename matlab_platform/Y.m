% =========================================================================
% 教学操作说明：兼容旧教程的路径初始化入口。
% 使用方法：
%   在项目根目录运行 Y，等价于执行 setup_bcu_paths()。新手可以用它快速
%   初始化；写新实验脚本时，建议使用 paths = setup_bcu_paths() 并保留 paths。
% 参数：
%   本文件是脚本，不接收函数形参。
% 返回 / 工作区结果：
%   在 base workspace 中生成 paths 结构体，并将 B3_MM 与项目自有 MATPOWER
%   目录加入 MATLAB path；命令窗口会显示所识别的项目根目录。
% 步骤：
%   1. 调用 setup_bcu_paths。2. 接收路径结构体。3. 打印根目录供人工核对。
% 单位：
%   路径字符串，无物理单位。
% 前置条件：
%   必须从完整项目目录运行；若要诊断路径问题，请直接调用 setup_bcu_paths()
%   并查看其返回值，不要修改本兼容入口来硬编码个人电脑路径。
% 研究与验证边界：
%   本文件只初始化路径，不运行潮流、暂态、CCT 或绘图，也不改变模型参数。
% =========================================================================
paths = setup_bcu_paths(); %#ok<NASGU>
fprintf('BCU_3M9B MATLAB paths initialized from:\n%s\n', paths.root);
