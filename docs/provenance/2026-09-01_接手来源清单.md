# 2026-09-01 接手来源清单

## 来源目录

`C:\Users\WangS\Desktop\26年第二学期学习文件\论文文献\literature_ntu_chatgpt_only\BCU_3M9B_tree`

本仓库建立于 `C:\Users\WangS\Documents\ChatGPT\项目接手\BCU_3M9B-development`。
来源目录顶层不是 Git 仓库；三个平台目录各自保留独立 Git 元数据，但本仓库只导入
其已提交 HEAD 的文件快照，不声称保留旧仓库完整历史。

## 平台基线

| 平台 | 来源提交 | 来源分支 | 来源工作区 | 本仓库用途 |
|---|---|---|---|---|
| `python_bcu_v2` | `4bc054c81b7193318ac2bf179a248dca3cc3341b` | `main` | 干净，`origin/main` 对齐 | 主开发平台 |
| `python_bcu` | `34ecbf974bbe27cf63d9ec45e39626b5e49b6f2c` | `main` | 有未跟踪 `results/`，未导入 | 兼容基线，只读 |
| `matlab_platform` | `035f1475fd92e5639ff9b7fb78eb678ed2976e1c` | `main` | `bcu_config.m` 有本地 `spm_cct` 改动，未导入 | 只读参考方程 |

MATLAB 的本地未提交改动为 `cfg.mode = "spm_cct"`，仅作为审计事实记录；快照取自
提交 HEAD，因此不会把该改动误写成已审阅的 MATLAB 版本。v1 的 `results/` 保留在
原目录，不复制到本仓库。

## 文件统计

| 平台 | HEAD 已跟踪文件数 | 快照目标 |
|---|---:|---|
| `matlab_platform` | 78 | `matlab_platform/` |
| `python_bcu` | 43 | `python_bcu/` |
| `python_bcu_v2` | 31 | `python_bcu_v2/` |
| 第一方 Markdown（来源树，排除 `C1_Matpower/**` 和 `.git/**`） | 21 | 以所在平台快照为准 |

第一方 Markdown 数量为本轮重新统计值；早期交接材料中的“19 篇”不再作为当前事实。

## 明确排除

- `C1_Matpower/`：MATPOWER 第三方依赖，由本地引导脚本接入；
- `*.mat`：大型或可重生的 MATLAB 工作区/基准；
- `results/`、`figures/`、缓存和临时文件：运行产物；
- 各来源仓库的 `.git/`：旧仓库元数据；
- 论文、截图、课程材料、凭据及上级目录内容：不属于代码平台快照。

根目录 `SOURCE_MANIFEST.csv` 记录每个导入文件的来源相对路径、目标路径、来源提交、
工作区状态、字节数、行数、SHA-256 和导入时间。个人绝对路径只出现在本审计说明中，
不会写入清单字段。

`matlab_platform/` 和 `python_bcu/` 是只读来源快照，清单哈希必须保持一致；
`python_bcu_v2/` 是本统一仓库指定的主开发平台，允许在基线哈希之上产生经过审查的
工作树改动。`scripts/Test-Docs.ps1` 对前两者执行严格哈希门禁，对 v2 仍检查文件存在、
扩展名和 Git 差异，并保留清单中的基线哈希用于溯源。

## 接手边界

本轮只修改本统一仓库。MATLAB、Python v1 和 Python v2 原始仓库保持不变；任何疑似
MATLAB MGP 复制错误、旧基线数字或 SPM 分支结论都必须经过代码路径和运行证据确认后，
才能升级为权威结论。
