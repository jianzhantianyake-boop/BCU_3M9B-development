# 2026-09-02 验证与远程同步收尾

## 本轮环境

| 项目 | 实际值 |
|---|---|
| Python | `C:\Users\WangS\AppData\Local\Programs\Python\Python312\python.exe`，3.12.1 |
| NumPy / SciPy / Matplotlib | 2.2.4 / 1.17.1 / 3.10.1 |
| MATLAB | R2024a Update 8（24.1.0.3050227） |
| MATPOWER | 本地 `matlab_platform/C1_Matpower/matpower7.1`，未纳入 Git |
| 集成仓库 HEAD（本轮最终核对） | `718fb79` |
| 远程 | `https://github.com/jianzhantianyake-boop/BCU_3M9B-development.git`，PRIVATE，默认分支 `main` |

环境审计返回 `OK`。MATPOWER 自带 `.mat` 测试工件仅存在于被忽略的本地依赖目录；本仓库未跟踪
任何 `.mat`、结果、缓存或凭据。

## 已实际运行

- MATLAB 只读 `export_spm_region.m`：原始 `Statable_Region_SPM.m` 找到 2 个平衡点（type-1
  UEP、SEP），导出 `validation/references/spm_region_v1.json`；导出文件声明
  `matlab_native_export`，不把导出本身计作交叉验证。
- Python SPM 核心：`test_spm_cuep.py` 5/5、`test_spm_trajectory.py` 4/4、
  `test_spm_region.py` 3/3、`test_mutations.py` 9/9、`test_reference_io.py` 6/6、
  `test_spm_xval_gate.py` 4/4。
- 统一入口 `scripts/run_all_validation.ps1`：最新报告
  `validation/reports/20260901_194757Z/summary.json` 中 10 项有 9 项 `PASSED`；第 10 项
  `run_full_xval.py` 退出码 1，因为其职责是拒绝未完成的八路径总门禁，不是崩溃重试。
- 阻尼驱动 `experiments/run_damping_study.py` 实测返回 `BLOCKED`（退出码 2），因为
  `spm_cct` 尚未达到 `MATLAB_XVAL_FULL`；未创建结果目录。
- `scripts/Test-Docs.ps1` 返回 `DOCS_OK manifest=143 tracked=190`，`git diff --check` 无错误。
- v2 全量单元测试最新为 33/33 `OK`；阻尼驱动接口测试为 2/2 `OK`。字段修正后再次实跑
  `experiments/run_damping_study.py` 返回 `BLOCKED`（退出码 2、24 个案例为空），且不创建
  `experiments/results/`。

## 当前八路径状态

统一报告 `validation/reports/full_xval_latest.json` 当前为：7 条
`MATLAB_XVAL_FULL`、1 条 `UNVERIFIED`（SPM 自足 CUEP 能量门禁）。严格 Python
fault-network DAE 能量峰值约 4.64941，而当前自足候选
`E_critical=7.57668`，因此没有伪造有限 CCT。

本轮追加运行：MATLAB `fault1` 原生 DAE 固定检查点导出成功（7 点，网络代数残差最大
`3.02e-10`）；Python 使用显式 `Epu` 严格 DAE 和网络分支 warm-start 完成 28/28
逐变量检查，最大误差约 `2.70e-9`。旧 `spm_numerical_v1.json` 仍因 `fault1` 残差约
`13.6453` 被测试标为不可比历史参考，新 v2 参考才是当前交叉验证依据。

稳定域追加运行：MATLAB `Statable_Region_SPM.m` 的 type-1 UEP 正/负稳定流形固定
采样参考成功导出；Python 约束切空间积分完成 20/20 项对照，最大误差约 `5.03e-7`，
因此 `spm_region` 升级为 `MATLAB_XVAL_FULL`。该证据覆盖登记的固定边界采样点，
不声称连续区域的无限密度覆盖。

SPM MGP 只读诊断：MATLAB `Cal_MM_CCT_SPM.m` 在 R2024a 中成功完成 15 次轨迹更新，
最终 MGP 约为 `[-0.7512, 1.8382, 1.9813]`，CUEP 发电机角与 Python 连续网络分支
一致。诊断同时保留 `x_init`、`Results_fsolve`、MGP 角/网络角/电压和疑似复制行存在性。
最终成功结果的 `voltage_MGP` 与 `theta_MGP` 不同，因此只能确认源码行存在，不能确认
该行在本次成功退出分支中执行。

本轮追加的 Python 诊断修正只调整 MGP 的故障逃逸种子：故障段时长固定至少为 MATLAB
登记的 `0.5 s`，并使用 `find_exitpoint` 的首次功率失配/相对速度点积过零，而不再使用
短轨迹中的最大失配点。该修正与 MATLAB 入口判据一致，但实测仍返回
`maximum MGP segments reached`（梯度范数约 `1.0747`、分支连续性约 `0.4159`），因此
它不能被解释为 MGP 已收敛；本次只保留为未闭环诊断证据。

## 来源和保护边界

原 MATLAB、Python v1、Python v2 来源仓库未被本轮修改。MATLAB 的
`Fun_Cal_MGP_SPM.m` 中 `voltage_MGP = theta_iter(no_MGP,:)` 仍只记录为疑似复制错误；
没有分支执行证据前不升级为已确认缺陷。v1 原目录未跟踪的 `results/` 仍保留。
