# 修改日志

## 2026-09-02

- 阶段性收尾（参考接入前）：第二个约简 type-1 候选已完成 Python/MATLAB R2024a 原生同窗复核，
  `E_critical=1.4927509038`、故障能量峰值 `5.5678499612`、LEA CCT `0.149 s`，
  两端网络/联合残差约 `5.1e-15`。Python 已将能量门禁失败后的有限候选选择器接入
  `solve_spm_cuep`，相关失败优先测试通过；`spm_cct` 当前更新为
  当时状态为 `MATLAB_XVAL_PARTIAL`，等待同坐标紧凑参考和 SPM 时域判据闭环。

- `spm_cct_v2.json` 已登记为同坐标 MATLAB 紧凑参考；统一报告对发电机角、网络角、
  网络电压、`E_critical`、故障能量峰值、LEA CCT、网络/联合残差、type-1 分类和无外部
  能量依赖共 10 项比较全部通过（最大差异 `1.84e-4`，来自不同能量采样步长）。
  `spm_cct` 更新为 `PHYSICS_VALIDATED + MATLAB_XVAL_FULL`；旧 `spm_cct_v1.json` 保留
  为混合坐标历史审计。阶段 7 阻尼研究按用户要求跳过。

- 阶段性收尾前新增第二个约简 type-1 候选的双端原生复核：Python 与 MATLAB R2024a
  均得到正电压 SPM 联合根，`E_critical≈1.4927509038`、网络残差约 `5.1e-15`；
  Python 自足 LEA `0.149 s` 小于现有 reduced REA `0.2433 s`。该候选尚未接入默认
  多候选选择器，故 `spm_cct` 和八路径总门禁暂不升级；详细证据见
  `docs/provenance/2026-09-02_第二SPM候选原生复核.md`。

- 为 `SpmCuepResult` 增加稳定的机器可读 `failure_code`：能量门禁拒绝统一记录为
  `ENERGY_GATE_EXCEEDED`，网络分支、候选缺失和联合残差失败也分别编码；八路径报告
  同时保留该代码和原有的人类可读限制说明，避免批量研究把阻塞误判为崩溃或成功。

- 以固定随机种子执行 500 个 SPM 联合初值审计（含 MGP 角度/物理网络种子和随机扰动）：
  80 个启动点严格收敛，去重后只有 SEP 与同一个正电压 type-1 CUEP；其
  `E_critical=7.576683267749164`、网络残差 `5.94e-15`、平衡残差 `7.95e-16`，
  未发现低于严格故障峰值的替代物理根。该有限搜索证据已写入阶段 4/7 审计记录，
  不把 `spm_cct` 升级为完整闭环。
- 逐字复现 MATLAB `Fun_SEPfslove_SPM.m` 的 Q 残差并与物理网络方程对照，确认其在
  正电压域是物理 Q 残差除以对应电压，零点等价；该差异不是 `3.3757` 与 `7.5767`
  分叉的来源，坐标混用仍是唯一已确认的 MATLAB 结果差异。

- 新增 `spm_homotopy.py` 与 `run_spm_homotopy_audit.py`：从 MATLAB MGP 的完整物理网络
  状态执行 100 步联合同伦追踪，最终 type-1 根的 `E_critical=7.5766832677491305`，
  网络/平衡残差约 `1.77e-14/7.33e-16`，且不读取外部临界能量。该独立证据仍未找到
  低于严格故障峰值的物理分支，故 `spm_cct` 继续保持 `UNVERIFIED`。
- 新增同伦测试后的完整统一回归目录为 `validation/reports/20260902_025831Z/`：15 项中
  14 项退出码为 0；同伦测试 2/2、SPM 参考门禁 7/7、变异门禁和阻尼研究门控均通过。
  `run_full_xval.py` 按约定以退出码 1 拒绝 `7 FULL + 1 UNVERIFIED`，没有被误报为全部
  八路径完成。

- SPM MGP 继续移植：新增 MATLAB 同源解析 Newton 网络校正器和 Ep4 多段路径积分；
  Ep4 在 `PathEnergyCal=20` 时与 MATLAB 射线采样逐点一致（能量误差约 `1e-10`），
  而 CUEP 默认仍保持 `PathEnergyCal=0`。MGP 逃逸种子改为严格 SPM fault1 DAE 的
  点积过零（`tunit=1e-4`），不再用约简轨迹冒充 SPM 证据。
- MGP 射线在 Python 中仍需较长时间且默认案例尚未取得可接受的自足临界能量；
  `spm_self_contained_cct` 增加可记录的 `max_segments` 预算，八路径报告暂以单条
  轨迹的有界诊断运行并保持 `spm_cct=UNVERIFIED`，不把部分运行升级为闭环。
- 对固定 MATLAB CUEP 发电机角做 501 个随机/扰动初值的网络根枚举，仅复现同一个正
  电压根（物理 `E_critical=7.576683268`）。该有限扫描不宣称穷尽全部数学根，但目前
  没有证据支持把历史混合坐标 `3.3757` 当作同一物理分支。
- 修正 MGP 外层过大更新阈值：应比较“射线更新点—本条轨迹起点”，而不是“更新点—
  轨迹末端”。修正后 Python 15 条轨迹进入与 MATLAB 相同的重复状态分支，MGP 角约为
  `[-0.75120823, 1.83824291, 1.98132835]`；联合 CUEP 网络残差约 `2.3e-12`，但
  `E_critical=7.57668327 > 5.56766636`，阶段 4 仍保持未完成。

- 对 MATLAB 15 条 MGP 轨迹的全部 30 个 `update_delta/end_delta` 初值做物理网络连续
  求解和联合平衡根审计：去重后只有一个满足残差门限的正电压 type-1 根，
  `E_critical=7.576683268`；另一个 type-2 近似点的网络/平衡残差约为 `6.8e-2/2.9e-2`。
  该有限初值审计未发现低于故障能量峰值的替代物理分支，不能宣称穷尽所有数学根，
  也不改变 `spm_cct=UNVERIFIED`。

- `enumerate_spm_equilibria(grid_points=101)` 的 101×101 reduced 初值网格仍只发现 SEP
  与同一个 type-1 根；该加密搜索没有发现低能量候选，但仍按有限搜索证据记录。

- 对完整 SPM 发电机—网络联合方程追加 152 个有限初值/扰动启动，覆盖 SEP/CUEP、MGP
  更新点、轨迹末端和随机扰动。严格残差去重后仅有 SEP（`E≈0`）与同一个正电压
  type-1 CUEP（`E_critical=7.576683267749161`，联合残差约 `3.8e-15`）；没有发现
  低于故障能量峰值的替代物理分支。该结果不是全根穷举证明，`spm_cct` 仍为
  `UNVERIFIED`。
- 新增可重复的联合根审计种子接口和先行测试：`seed_deltas` 重放 MGP 角度，
  `seed_states` 接受完整校正网络角/电压，拒绝缺失或非有限网络值。使用 MGP 迭代报告与
  物理诊断报告重放 152 个启动点后，得到 25 个严格收敛启动点、2 个唯一物理解（SEP 与
  `E_critical=7.576683267749164` 的 type-1 根）；报告明确 `used_external_ecritical=false`。
  该有限审计仍不构成全根证明，`spm_cct` 保持 `UNVERIFIED`。
- 独立复现 MATLAB `Fun_SEPfslove_SPM.m` 的网络 Q 残差，确认其相对物理 AE 方程只是按
  正网络电压归一化，零点等价，不能作为 `3.3757` 与 `7.5767` 分叉的解释；文档明确
  将该现象与已确认的 CUEP COI 平移混用分开记录。
- 扩大 reduced 初值搜索发现的额外 type-1 结果均是同一物理解的 `2π` 周期副本；联合
  根审计改为按相对发电机角、网络相对角和电压幅值去重，新增周期副本回归测试通过，
  152 点固定重放仍得到 2 个唯一物理解。

- 在提交 `a9cd242` 的干净工作区上完整运行默认 SPM MGP（1000 条轨迹预算）：15 条轨迹后
  以 `MGP ray update repeated` 收敛，MGP 网络残差 `4.53e-12`；联合 type-1 CUEP 的
  平衡/网络残差为 `1.17e-13/2.21e-12`，但 `E_critical=7.576683267750361` 仍高于
  故障能量峰值 `5.567666364057937`，所以 `spm_cct` 按设计保持 `UNVERIFIED`，没有输出
  伪造 CCT。该完整运行证据已写入阶段 4/7 审计记录。

- 以上完整运行审计以提交 `a947e5d` 推送到私有远程；推送前后工作区均保持干净，
  `HEAD...origin/main=0 0`。

- 为避免 SPM 恒阻抗网络冷启动偶落到零电压数学根，`solve_spm_network` 增加了仅在“无显式
  初值且无恒功率注入”条件下启用的线性复数网络 fallback；显式坏初值仍按失败返回。新增
  冷启动物理分支测试先失败后通过，完整 `test_spm_cuep.py` 为 9/9、联合根审计为 5/5、
  变异门禁为 9/9；该修正不改变 `E_critical=7.57668327 > 5.56766636` 的阶段 4 门禁结论。

- 阶段 6 关键方程变异测试实际运行 9/9 通过，并纳入统一回归入口；覆盖 Kron、摆动方程、
  故障线路、COI、发电机顺序、能量角差、SPM P/Q、网络电压/角度和分支连续性错误。
  该结果只证明受控变异可被捕获，不提升 `spm_cct` 的物理验证状态。

- 修复所有 PowerShell 维护脚本在 Windows PowerShell 5.1 下的默认根目录推导：不再在参数
  默认值中依赖可能为空的 `$PSScriptRoot`，改为在脚本体内从自身路径解析仓库根目录；
  `Test-Docs.ps1`、统一回归入口及其余维护脚本的真实启动/参数解析检查均通过。

- 完整统一回归实跑（报告目录 `validation/reports/20260902_001413Z/`）：13 项中 12 项
  退出码为 0；`run_full_xval.py` 以退出码 1 正确拒绝 `7 FULL + 1 UNVERIFIED` 的八路径
  总门禁。其后的 SPM 参考门禁 7/7、关键方程变异测试 9/9 和阻尼驱动接口测试 3/3
  全部通过；24 案例因 `spm_cct` 未完成而保持 `BLOCKED`，未生成结果。

- 修正阻尼研究驱动的 `gamma` 映射：研究定义是相对于基准阻尼比 `0.1` 的倍率，旧实现
  错把 `0.5/1.0/1.5` 直接作为阻尼比。新增回归测试先复现该错误，再改为
  `0.1 × gamma`，驱动测试现为 3/3 通过；八路径门禁未通过前仍只返回 `BLOCKED`，未运行
  24 个案例。

- 以 fault DAE `tunit=1e-4`、1001 个输出点复核严格故障能量峰值，得到
  `5.56783827`（约 `t=0.2975 s`），与原登记值 `5.56766636` 一致；最大网络残差约
  `8e-12` 且网络电压保持正值，故 `E_critical=7.57668` 超过峰值的结论不是采样伪影。

- Python CUEP 参考门禁新增 raw fsolve 网络角加回 `cuep_frame_shift` 后的物理能量重算，
  记录 `corrected_raw_network_residual=1.1647e-10`、`corrected_raw_e_critical=7.576683268`；
  该诊断与 MATLAB 五项势能快照一致，但仍因 `7.57668 > 5.56767` 保持 `spm_cct=UNVERIFIED`，
  不把历史 projected `3.3757` 作为自足输入。

- 修正 v2 严格 SPM DAE 的方程实现：代数网络和发电机注入显式使用 `preset.Epu`，不再
  复用把发电机幅值隐式当成 1 的 v1 辅助函数；后故障映射对重新出现的故障母线使用已
  求解的后故障 SEP warm-start，不插入零值。
- 新增 MATLAB `fault1` 原生 DAE 固定检查点导出器和不可变参考
  `validation/references/spm_numerical_v2.json`；旧 v1 `fault.traj` 参考因方程残差约
  `13.6453` 保留为历史不可比证据。Python 逐点对照 7 个时间点 × 4 组变量共 28/28，
  最大误差约 `2.70e-9`，统一报告将 `spm_numerical` 升级为 `MATLAB_XVAL_FULL`。
- 新增 v2 fault1 参考可比性测试；核心 SPM DAE、轨迹、参考读取和交叉验证门禁测试均
  通过。当前八路径状态为 7 条 `MATLAB_XVAL_FULL`、1 条 `UNVERIFIED`（SPM 自足 CUEP
  能量门禁）。
- 新增 MATLAB 稳定流形固定检查点导出器和 `spm_region_manifold_v1.json`；Python 以
  约束切空间反向积分复现两条 type-1 分支，2 × 5 个时间点的发电机角、网络角、电压
  和网络残差共 20/20 通过，最大误差约 `5.03e-7`。`spm_region` 升级为
  `MATLAB_XVAL_FULL`，但连续区域的无限密度覆盖仍明确列为限制。
- MATLAB MGP 只读诊断完成：`Cal_MM_CCT_SPM.m` 成功执行 15 次轨迹更新，MGP 约为
  `[-0.7512, 1.8382, 1.9813]`，并保留 `x_init`、`Results_fsolve` 及网络状态；源码中的
  `voltage_MGP = theta_iter(no_MGP,:)` 仅确认存在，最终成功结果仍使用真实电压向量，
  因此不把该行升级为已确认缺陷。
- Python MGP 逃逸种子已对齐 MATLAB 入口判据：固定至少 `0.5 s` 故障轨迹，并取首次
  功率失配/相对速度点积过零，不再取短轨迹最大失配点。该修正实测仍以
  `maximum MGP segments reached` 退出（梯度范数约 `1.0747`、分支连续性约 `0.4159`），
  所以 `spm_cct` 继续保持 `UNVERIFIED`，没有把诊断性改动写成闭环完成。
- 修正 SPM 联合平衡中的公共速度偏移：`omega_coi` 和发电机残差按阻尼总和
  `PCOI=Σd·omega_coi` 计算，避免旧实现误用惯量总和；该修正通过 CUEP 回归测试，未改变
  当前 `E_critical=7.57668 > 5.56767` 的严格能量门禁结论。
- 修正阶段 7 阻尼批量驱动对统一 SPM 返回结构的字段引用：`spm.found` 统一改为
  `spm.converged`，并新增驱动测试；当前八路径门禁仍为阻断状态，批量入口不会创建结果目录。
- 最终回归核对：v2 全量单元测试最新 36/36、阻尼驱动测试 2/2 通过；统一入口最新报告
  `validation/reports/20260901_194757Z/summary.json` 为 9 项 `PASSED`、八路径报告项按设计
  以退出码 1 拒绝未完成的 `spm_cct` 总门禁。
- 能量重构修正后重新生成 `validation/reports/full_xval_latest.json`：当前门禁限制项为
  `E_critical=7.57668 exceeds fault energy peak=5.56767`，其余七条路径状态不变。
- 追加 MATLAB 只读物理 CUEP 快照诊断：将 `Results_fsolve` 原始网络角减去实际 CUEP 的
  COI 平移后，网络残差约 `4.17e-15`，五项势能合计 `E_critical=7.57668`，与 Python
  自足候选一致。该结果确认历史 `3.3757` 来自混合坐标/状态取值，但因仍超过严格故障
  能量峰值 `5.56767`，`spm_cct` 继续保持 `UNVERIFIED`；诊断器只读且不修改 MATLAB
  `B3_MM`。
- MATLAB 物理快照同时确认了一个独立的坐标混用现象：`postfault.deltacoi=-1.997835`，
  但 `postfault.CUEP_net_theta` 的写入使用 base 变量 `deltacoi=-0.295690`，存储角与该
  平移完全一致，和实际 CUEP 平移相差 `4.16939`。这解释了 projected 参考网络残差
  `11.3454`；本轮只记录证据并要求后续显式字段修正，没有修改原 MATLAB 管线。
- Python `inspect_spm_cuep_reference` 增加 raw fsolve 角度加回 `cuep_frame_shift` 后的
  物理残差输出，并以失败优先测试固定该门禁；SPM 交叉验证门禁当前 7/7 通过。修正后的
  物理残差低于 `1e-6`，但历史 projected 字段仍保留为不可比较证据。

- 严格 SPM fault-network DAE 能量序列已接入 `spm_energy.spm_fault_energy_series`；不再用
  reduced 轨迹代理故障能量峰值。现按 MATLAB 同窗 0.5 s 对每个发电机角重构故障后网络，
  默认案例峰值约 `5.56767`；自足 CUEP 当前候选 `E_critical=7.57668` 因超过峰值而保持
  `UNVERIFIED`。此前直接零占位得到的 `4.64941` 已废弃。
- MATLAB 原生 `Statable_Region_SPM.m` 只读运行完成，导出
  `validation/references/spm_region_v1.json`：2 个平衡点（type-1 UEP + SEP），包含
  发电机角、网络角/电压、分类和分支标识；导出本身不计作交叉验证。
- Python SPM 区域枚举增加 SEP 到候选点的连续 warm-start 网络求解，恢复与 MATLAB
  两个平衡点的逐项匹配；统一报告将 `spm_region` 更新为
  `MATLAB_XVAL_PARTIAL`，稳定域抽样/边界曲线仍未覆盖。
- 核心测试（SPM CUEP 5/5、轨迹 4/4、参考读取 6/6、区域门禁 1/1）通过；统一报告仍
  保留 5 条 `MATLAB_XVAL_FULL`，未把 `spm_cct`、`spm_numerical` 或区域部分证据升级为完整路径。
- 统一回归入口本轮 10 项中 9 项实际通过；`run_full_xval.py` 按总门禁约定退出 1。
  GitHub CLI 已确认 `jianzhantianyake-boop` 登录，目标私有仓库创建并推送成功，`main`
  与 `origin/main` 差异为 `0 0`。

## 2026-09-01

- 建立 `BCU_3M9B-development` 统一开发仓库骨架；
- 从三个来源仓库的已提交 HEAD 导入 78/43/31 个文件，排除 v1 `figures/` 运行图；
- 生成 `SOURCE_MANIFEST.csv`，本轮第一方 Markdown 重新统计为 21 篇；
- 增加来源审计、环境审计、MATPOWER 引导和统一回归入口；
- 建立八路径唯一状态矩阵，明确五条完整、SPM CCT 部分闭环、两条 SPM 未完成；
- MATLAB R2024a 本轮实际运行 `Cal_MM_CCT_SPM.m` 成功：记录 Exit point、MGP、CUEP，
  `E_critical=3.3756797086769623`（MATLAB SPM 管线）和 `LEA CCT=0.2053 s`；
  诊断器记录 MGP 分支执行及 `voltage_MGP=theta_iter(no_MGP,:)` 疑似复制行存在。
- 从只读 MATLAB `.mat` 快照生成五个紧凑 JSON 参考，清单记录源文件 SHA-256；MATLAB
  导出器已改为保留已有 AVAILABLE 参考，避免导出 SPM 时覆盖其他路径。
- Python 3.12 初次通过 `test_reference_io.py`（4 项）、`test_spm_cuep.py`（3 项）、
  `test_spm_region.py`（1 项）和 `test_mutations.py`（9 项）；当时统一报告为 5 条
  `MATLAB_XVAL_FULL`、`spm_cct=UNVERIFIED`、`spm_numerical=APPROXIMATE`、
  `spm_region=UNVERIFIED`。自足 SPM 当前分支得到 `E_critical=7.57668`，高于本次
  Python 旧版代理计算的故障能量峰值 `6.46886`，未伪造 CCT；后续报告已增加 fault1
  参考可比性门禁，严格 SPM DAE 峰值见本日后续记录。
- 已阅读 Claude 早期交接、验证覆盖矩阵、Python fork 修改日志和 MATLAB 可靠性文档；
  它们只作为历史背景，未被当作本轮执行证据，原来源仓库未修改。
- 阶段 4 继续：SPM 网络求解拒绝零电压数学根；CUEP 网络同伦细化到 256 个 warm-start
  步，当前物理一致坐标残差约 `4e-15`、平衡残差约 `1.3e-13`、分支最大步长约
  `1.99e-2`，但 `E_critical=7.57668` 仍超过当时代理故障能量峰值 `6.46886`。
- MATLAB 紧凑参考增加 raw fsolve 网络状态和坐标平移字段；统一报告明确指出 raw/projected
  网络角最大差约 `1.70`，因此 `3.3757` 是历史混合坐标数值，不能直接作为物理闭环验收。
- 修复统一回归入口的 Windows 子进程启动方式后，计划中的 10 个任务中 9 个实际通过；
  `run_full_xval.py` 按设计以退出码 1 保留三条未完成 SPM 路径，未被误报为回归失败。
- 修复 SPM 网络冷启动在恒阻抗、零恒功率场景下误选零电压数学根：仅在隐式冷启动且
  常规根不满足物理电压条件时，从复数线性节点方程恢复正电压分支；显式坏初值仍按原
  失败处理。新增测试先红后绿，完整 `test_cuep.py` 为 9/9。
- 阶段 5 初次诊断：增加 MATLAB SPM 固定检查点导出器和 Python 检查点选择器，修复
  输出网格端点少一个样本的问题；初始长时 Radau 运行曾因代数上下文不一致产生非有限
  电压，随后已在本日后续记录中修正并重新验证。
- 阶段 5 继续：新增失败优先测试后，将 `spm_dae` 代数上下文改为 MATLAB SPM 同源的
  `Yfull_mod` 恒阻抗网络和零恒功率负荷；Python 故障/故障后长时 Radau、RK45 均完成，
  最大代数残差约 `7.2e-13`。新增固定参考可比性门禁发现 MATLAB `fault1` 检查点最大
  网络残差约 `13.6453`，统一报告将 `spm_numerical` 改为 `NOT_COMPARABLE`，未进行
  跨平台误差统计。
- 阶段 4/5 继续：能量峰值计算改用完整 fault-network SPM DAE 轨迹并按母线号映射删去的
  故障母线；严格峰值为 `4.82202`，因此自足 CUEP 的 `E_critical=7.57668` 仍结构化
  失败。旧的 `6.46886` 仅保留为 reduced/postfault 代理历史数值。

## 2026-09-02（接手后续）

- 在提交 `d9fc322` 后完成统一回归实跑，报告目录为
  `validation/reports/20260902_015622Z/`：14 项中 13 项退出码为 0；`run_full_xval.py`
  以退出码 1 保留 7 条 `MATLAB_XVAL_FULL` + 1 条 `UNVERIFIED` 的诚实总门禁状态。
- 同一轮中 `test_cuep.py` 9/9、`test_spm_joint_audit.py` 5/5、变异测试 9/9、SPM
  参考门禁 7/7、阻尼驱动接口 3/3；没有启动 24 个阻尼案例，也没有写入伪造结果。
- 新增 `matlab_platform/verify/export_spm_energy_series.m` 作为只读能量诊断器，并在
  MATLAB R2024a 中按严格 `PathEnergyCal=0` 逐点重算 `Tfault=0.5 s` 的 `5000` 个
  故障轨迹点。原生 MATLAB 峰值为 `5.567849961155664`（`t=0.2973 s`），同窗 Python
  为 `5.5678499591622215`，差约 `2.0e-9`；这确认 `7.5766832677` 超过故障能量峰值
  不是采样、角度展开或 Python 能量实现误差。此前 `7.6195895469` 明确归属于
  `PathEnergyCal=20` 的 MGP 射线积分运行，不作为严格 CCT 峰值。`spm_cct` 因此仍为
  `UNVERIFIED`，未生成 CCT 或失败占位值。
- MATLAB MGP 只读追踪器新增网络角/电压字段并完成 15 条轨迹重跑；最终更新状态为
  `theta=[-0.697282,-0.280003,-0.762687,1.533584,1.600298,1.741643]`、
  `voltage=[0.615802,0.301514,0.597046,0.584887,0.654448,0.775594]`，与物理
  type-1 CUEP 连续相邻且无 `2π` 跳跃，故周期副本不构成低能量替代分支。
- 在最新提交 `ab8c457122f3f79e17a49e018e608dcf4a16e05e` 上重新运行环境审计：Python
  `3.12.1`、NumPy `2.2.4`、SciPy `1.17.1`、Matplotlib `3.10.1`、MATLAB `R2024a
  Update 8` 和本地 MATPOWER 路径均可用，仓库状态为空，审计状态 `OK`。列出的 `.mat`
  文件只来自被忽略的 MATPOWER 第三方目录。
- 重跑 500 个 SPM 联合初值审计：80 个启动点收敛、去重后 2 个根，唯一正电压 type-1
  CUEP 的 `E_critical=7.576683267749164`，未读取外部临界能量；文档门禁
  `DOCS_OK manifest=143 tracked=199`，阶段 4/6/7 完成状态不变。
- 新增只读 MATLAB 诊断器 `matlab_platform/verify/export_spm_energy_gate.m`，记录正式
  SPM CUEP/CCT 前 `PathEnergyCal` 被重置为 `0`；物理一致 CUEP 为
  `7.5766832677097984`、同窗故障峰为 `5.5678499611556642`，历史 projected 字段的
  `3.3756797086769623/0.2053 s` 仍因网络残差 `15.9067728021` 只作混合坐标诊断。
  强制 `PathEnergyCal=20` 的 Python 对照为 `E_critical=2.0524491157`、越界约
  `0.222 s`，证明这是不同势能泛函而非合法修复；`spm_cct` 继续 `UNVERIFIED`，阶段
  7 未启动。
- 复核一个网络角 `2π` 展开候选：形式能量可降至 `3.3824125826`，但 128 段从 SEP 的
  连续求解终点仍为主值，和该人为展开相差 `±6.28318531`；`0.0397` 仅是主值副本
  的步长，不能当作物理分支连续性。该候选未进入参考或 CCT 门禁。
- 新增恒阻抗网络唯一性不变量测试：对 8 组固定随机发电机角，将复数线性节点解与
  SPM 极坐标求解逐元素比较，残差均小于 `1e-10`。这把交接中的“11+ 网络解”限定为
  联合发电机根、零电压数学根或周期表示，不再把它解释为多个正电压物理网络分支。
- 复核一个网络角 `2π` 展开候选：形式能量可降至 `3.3824125826`，但 128 段从 SEP 的
  连续求解终点仍为主值，和该人为展开相差 `±6.28318531`；`0.0397` 仅是主值副本
  的步长，不能当作物理分支连续性。该候选未进入参考或 CCT 门禁。
