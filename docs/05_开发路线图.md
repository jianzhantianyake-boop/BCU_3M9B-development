# 05 开发路线图

## 已完成且有证据

- 三个平台已从已提交 HEAD 建立统一快照并生成来源清单；
- v2 已具备 reduced CUEP、SPM 网络代数解、五项势能和连续 SPM DAE 原型；
- 五条路径已有指定输出的历史 MATLAB 交叉验证记录；本轮已在 MATLAB R2024a 中重跑
  SPM 管线并导出紧凑参考，历史五条参考也已从只读 `.mat` 快照重新生成。该重跑不等于
  五条路径的交叉验证在本轮全部重做。

## 当前唯一主里程碑：SPM 自足 CUEP 分支选择

验收必须同时满足：不读取 MATLAB `E_critical`；联合发电机—网络残差小于登记容差；网络
状态沿物理轨迹连续；默认案例独立得到约 `E_critical=3.3757` 且小于故障能量峰值
`5.568`；得到约 `LEA CCT=0.2053 s`；`LEA < REA`；全部既有回归不退化；并先确认
MATLAB MGP 实际退出分支，不逐行盲目移植疑似错误。

## 后续候选

主里程碑通过后，完成 SPM 数值轨迹固定检查点、SPM 稳定域和八路径变异测试总门禁。门禁
通过后再执行 24 案例非均匀阻尼研究。39 母线研究另立计划，待 H、Xd′ 和动态参数权威
核对前不形成论文定量结论。

## 当前执行检查点

- MATLAB SPM：`PHYSICS_VALIDATED` 的公式/机制参考已导出；MGP/CUEP 诊断记录显示
  `Fun_Cal_MGP_SPM.m` 的疑似复制行实际存在且该分支执行过，但这仍不是已确认缺陷。
- Python SPM：自足 CUEP 接口、结构化失败返回和变异测试已加入；默认案例尚未满足
  `E_critical < fault_energy_peak`，因此不能写成自足 CCT 已闭环。
- 研究阶段：阶段 7 暂停，直到阶段 6 的八路径总门禁和 SPM 三条路径验收完成。
