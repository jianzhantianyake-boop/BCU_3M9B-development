# 03 验证覆盖矩阵

本表是八条主路径的唯一权威状态。检查项数量不是路径覆盖率；每个数值必须绑定对象、
范围、参考版本、误差和限制。

| 路径 | 权威状态 | 当前证据边界 |
|---|---|---|
| `reduced_cct` | `PHYSICS_VALIDATED` + `MATLAB_XVAL_FULL` | T3 8/8 仅属于该路径；核心误差约 `1e-10~1e-11` |
| `reduced_numerical` | `MATLAB_XVAL_FULL` | 对照故障段末端 `thetac`，误差约 `1.2e-11`；不是完整逐点轨迹 |
| `reduced_region` | `MATLAB_XVAL_FULL` | 平衡点集合误差约 `4.5e-11`；稳定边界曲线需单独比较 |
| `spm_cct` | `UNVERIFIED` | 网络解、功率和给定 `E_critical` 的机制有历史对齐证据；MATLAB 紧凑 CUEP 参考按正确 SPM 网络方程计算的 projected 网络残差约 `11.3454`，Python 自足候选 `E_critical=7.57668` 超过 MATLAB 同窗 0.5 s 重构故障能量峰值约 `5.56767`，故未通过门禁 |
| `spm_numerical` | `MATLAB_XVAL_FULL` | 使用与 `fault1` DAE 同源的 `spm_numerical_v2.json` 固定检查点；7 个时间点 × 4 组变量共 28/28，通过最大误差约 `2.70e-9`；旧 v1 参考仍保留为不可比历史证据 |
| `spm_region` | `MATLAB_XVAL_FULL` | 平衡点及 type-1 稳定流形固定检查点已逐项对照；2 条分支 × 5 个时间点共 20 项，最大误差约 `5.03e-7`；连续区域的无限密度覆盖仍不声称 |
| `two_machine_region_3d` | `MATLAB_XVAL_FULL` | 平衡点集合误差约 `6.2e-13`，D2=0.5 已对齐 |
| `two_machine_region_3d_gfl` | `MATLAB_XVAL_FULL` | 平衡点集合误差约 `1.4e-11` |

统一汇总措辞：七条路径完成指定输出的 MATLAB 交叉验证；`spm_cct` 是额外的一条未闭环
路径。`run_full_xval.py` 当前为 `7` 条完整路径，仍不等于八条路径全部完成。

本轮执行记录（2026-09-02）：MATLAB R2024a 从本仓库工作目录成功启动并运行
`Cal_MM_CCT_SPM.m`，导出 `spm_cct_v1.json`；观测到 MGP、CUEP 和
`CCT(LEA)=0.2053 s`。Python 3.12 也已启动并完成参考 schema、SPM 原型和变异测试。
这些运行证明了环境与导出链可用，但不改变 `spm_cct` 的权威状态：Python 自足求解仍因
当前分支得到的 `E_critical=7.57668` 超过本次 Python 严格 SPM fault-network DAE 计算的
故障能量峰值 `5.56767`
而未通过物理门禁。最新只读诊断记录了 MATLAB MGP 输出（约
`[-0.7512, 1.8382, 1.9813]`）和最终 `Results_fsolve`；源码中的
`voltage_MGP = theta_iter(no_MGP,:)` 确实存在，但该次成功结果的电压向量与角度向量不同，
表明最终走的是重复状态更新分支，不能据此认定该行实际执行。紧凑参考同时记录了 MATLAB
raw fsolve 网络角与 projected 网络角，两者最大坐标差约 `1.70`；因此 MATLAB 的
`E_critical=3.3757` 只能作为历史混合坐标参考，不能替代物理一致坐标结果。统一报告
`validation/reports/full_xval_latest.json` 当前为
`7 MATLAB_XVAL_FULL + 1 UNVERIFIED`；其中部分 FULL 的误差和覆盖范围仍来自历史交叉验证
证据，本轮新增并实际重跑了 SPM 数值轨迹和稳定流形固定检查点。

阶段 5 收尾记录：MATLAB 已重新导出与 `fault1` DAE 同源的固定轨迹检查点
（`t=0、0.1、0.2、0.2053、0.21、0.25、0.3 s`），Python 端使用显式 `Epu`、
故障网络物理分支 warm-start 和 COI 相对角速度完成逐变量对照。7 个时间点的角度、
角速度、网络角和网络电压共 28/28 通过，最大误差约 `2.70e-9`，因此 `spm_numerical`
已升级为 `MATLAB_XVAL_FULL`。旧 `spm_numerical_v1.json` 的 `fault.traj` 网络列在
`fault1` 方程下残差约 `13.6453`，仍保留并由测试明确标为历史不可比，不能覆盖新证据。
`spm_region` 已完成登记的平衡点与稳定流形固定采样点对照；限制是该证据不等于连续
区域的无限密度覆盖。
