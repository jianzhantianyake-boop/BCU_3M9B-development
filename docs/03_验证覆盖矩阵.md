# 03 验证覆盖矩阵

本表是八条主路径的唯一权威状态。检查项数量不是路径覆盖率；每个数值必须绑定对象、
范围、参考版本、误差和限制。

| 路径 | 权威状态 | 当前证据边界 |
|---|---|---|
| `reduced_cct` | `PHYSICS_VALIDATED` + `MATLAB_XVAL_FULL` | T3 8/8 仅属于该路径；核心误差约 `1e-10~1e-11` |
| `reduced_numerical` | `MATLAB_XVAL_FULL` | 对照故障段末端 `thetac`，误差约 `1.2e-11`；不是完整逐点轨迹 |
| `reduced_region` | `MATLAB_XVAL_FULL` | 平衡点集合误差约 `4.5e-11`；稳定边界曲线需单独比较 |
| `spm_cct` | `MATLAB_XVAL_PARTIAL` | 网络解、功率、势能及给定 `E_critical` 的 CCT 机制已对齐；自足 CUEP 未闭环 |
| `spm_numerical` | `APPROXIMATE` / `UNVERIFIED` | 尚无完整 MATLAB 固定检查点对照 |
| `spm_region` | `APPROXIMATE` / `UNVERIFIED` | 尚无完整 MATLAB 平衡点/区域对照 |
| `two_machine_region_3d` | `MATLAB_XVAL_FULL` | 平衡点集合误差约 `6.2e-13`，D2=0.5 已对齐 |
| `two_machine_region_3d_gfl` | `MATLAB_XVAL_FULL` | 平衡点集合误差约 `1.4e-11` |

统一汇总措辞：五条路径完成指定输出的 MATLAB 交叉验证；`spm_cct` 是额外的一条部分
闭环路径；另外两条 SPM 路径尚未完成。`run_full_xval.py` 的 `5/5` 不等于八条路径
全部完成。

本轮执行记录（2026-09-01）：MATLAB R2024a 从本仓库工作目录成功启动并运行
`Cal_MM_CCT_SPM.m`，导出 `spm_cct_v1.json`；观测到 MGP、CUEP 和
`CCT(LEA)=0.2053 s`。Python 3.12 也已启动并完成参考 schema、SPM 原型和变异测试。
这些运行证明了环境与导出链可用，但不改变 `spm_cct` 的权威状态：Python 自足求解仍因
当前分支得到的 `E_critical=7.57668` 超过本次 Python 计算的故障能量峰值 `6.46886`
而未通过物理门禁。统一报告 `validation/reports/full_xval_latest.json` 当前为
`5 MATLAB_XVAL_FULL + 1 UNVERIFIED + 1 APPROXIMATE + 1 UNVERIFIED`；其中五条
FULL 的误差和覆盖范围仍来自历史交叉验证证据，本轮只重新生成并校验了紧凑参考。
